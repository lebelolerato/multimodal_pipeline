version: '3.8'

services:
  multimodal:
    build: .
    container_name: multimodal_pipeline
    volumes:
      - ./src:/app/src
      - ./data:/app/data
    working_dir: /app
    command: bash
    tty: true
